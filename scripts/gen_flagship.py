# -*- coding: utf-8 -*-
"""見本（フラッグシップ）: 03_統計_3級/01 を新品質基準で作り直す。
ヘッダー（バッジ/データ生成/インポート/プレビュー）は既存を流用し、本文を刷新。"""
import json, os
from nbbuild import md, code, write_nb

ROOT = "/Users/carlobroschi_imac/Workspace/Dev/Education_Python"
NB = "03_統計_3級/01_分散・標準偏差・標準化.ipynb"
ANS = "解答集/03_統計_3級/01_分散・標準偏差・標準化.md"

old = json.load(open(os.path.join(ROOT, NB), encoding="utf-8"))
badge, data_setup, imp, preview = old["cells"][0], old["cells"][1], old["cells"][3], old["cells"][4]

title = md(
    "# 統計3級-01. 分散・標準偏差・標準化\n\n"
    "**🎯 この章でできるようになること**\n"
    "- ばらつき（散らばり）を1つの数で表せる（分散・標準偏差）\n"
    "- *なぜ2乗するのか*・*なぜ√で戻すのか* を自分の言葉で説明できる\n"
    "- `÷n`（母分散）と `÷(n-1)`（不偏分散）の違いがわかる\n"
    "- 標準化（z得点）・偏差値で、条件のちがうデータを公平に比べられる\n\n"
    "**前提**：`01_Python基礎/05〜07`（NumPy・pandas・matplotlib）と統計4級の代表値　"
    "／　**所要**：約30分　／　**レベル**：統計検定3級相当")

cells = [badge, data_setup, title, imp, preview,

    md("## 0. なぜ「ばらつき」を測るの？（まず直感）\n\n"
       "2つのクラスのテスト。**平均はどちらも60点**。でも中身はまるで違います。"),
    code("A = np.array([58, 60, 62, 59, 61])   # 平均60・まとまっている\n"
         "B = np.array([30, 90, 60, 40, 80])   # 平均60・散らばっている\n"
         "print('平均: A =', A.mean(), ' B =', B.mean(), '（同じ！）')\n\n"
         "fig, ax = plt.subplots(1, 2, figsize=(9, 3), sharey=True)   # 2つ並べて描く\n"
         "ax[0].stem(A); ax[0].axhline(60, color='red', ls='--'); ax[0].set_title('Aクラス（まとまり）')\n"
         "ax[1].stem(B); ax[1].axhline(60, color='red', ls='--'); ax[1].set_title('Bクラス（散らばり）')\n"
         "plt.show()"),
    md("平均が同じでも散らばりは全然ちがう。**この「散らばり具合」を1つの数で表す**のが分散・標準偏差です。"),

    md("## 1. 偏差 → 分散 → 標準偏差（順番に・なぜ付き）\n\n"
       "1. **偏差** = 各データ − 平均（平均からどれだけズレているか）\n"
       "2. 偏差をそのまま平均すると…？ → **必ず 0**（＋と−が打ち消す）。だから **2乗** して符号を消す。\n"
       "3. **分散** = 偏差²の平均。ただし単位が「点²」になり直感的でない。\n"
       "4. **標準偏差** = √分散。単位が元（点）に戻り「平均から平均的に何点ズレるか」と読める。\n\n"
       "> ⚠️ **よくある間違い**：「偏差の平均」でばらつきを測ろうとする → 必ず0になり使えない。"),
    code("x = np.array([60, 70, 80, 90, 100])\n"
         "dev = x - x.mean()                       # 偏差\n"
         "print('偏差:', dev)\n"
         "print('偏差の合計:', dev.sum(), '← いつも0（だから2乗する）')\n"
         "var = (dev**2).mean()                    # 分散 = 偏差²の平均\n"
         "print('分散:', var)\n"
         "print('標準偏差:', np.sqrt(var).round(2), '← √で単位が元に戻る')"),

    md("## 2. シミュレーションで体感する\n\n"
       "標準偏差を変えてデータを作り、ヒストグラムの広がりを見比べます。"),
    code("rng = np.random.default_rng(0)\n"
         "for sd in [5, 15, 30]:                          # 標準偏差を3通り\n"
         "    d = rng.normal(60, sd, 1000)                # 平均60・標準偏差sd の1000個\n"
         "    print(f'設定 std={sd:2d} → 実際の標準偏差={d.std():.1f}')\n"
         "    plt.hist(d, bins=30, alpha=0.5, label=f'std={sd}')\n"
         "plt.legend(); plt.title('標準偏差が大きいほど横に広がる'); plt.show()"),
    md("**標準偏差が大きい＝ヒストグラムが横に広い＝散らばっている**、と体で覚えましょう。"),

    md("## 3. `÷n` か `÷(n-1)` か（つまずきポイント）\n\n"
       "- **`÷n`（母分散）**：手元のデータが“全体（母集団）”そのもののとき。\n"
       "- **`÷(n-1)`（不偏分散）**：標本から“全体のばらつき”を推定するとき。標本だと小さめに出やすいので n-1 で補正。\n\n"
       "> ⚠️ **事故が多い所**：pandas `.var()`/`.std()` は既定で **÷(n-1)**、NumPy `np.var`/`np.std` は **÷n**。"
       "同じデータでも値がズレます。"),
    code("s = pd.Series([60, 70, 80, 90, 100])\n"
         "print('pandas s.var()   :', s.var(), '  （÷(n-1) 不偏）')\n"
         "print('numpy  np.var(s) :', np.var(s), '  （÷n 母分散）')\n"
         "print('numpy  ddof=1    :', np.var(s, ddof=1), '  （pandasに合わせる）')"),

    md("## 4. 標準化（z得点）— 条件のちがうデータを公平に比べる\n\n"
       "数学（平均65・σ15）で80点 と 英語（平均70・σ8）で80点、**どちらがすごい？**\n"
       "「平均から標準偏差いくつ分はなれているか」で測るのが **z得点**：\n\n"
       "$$ z = \\frac{x - 平均}{標準偏差} $$"),
    code("for col in ['数学', '英語']:\n"
         "    z = (80 - df[col].mean()) / df[col].std()    # 80点のz得点\n"
         "    print(f'{col}で80点 → z = {z:.2f}')\n"
         "print('zが大きい科目のほうが、その中で相対的に上位')"),
    md("標準化すると平均0・標準偏差1になり、**単位や難易度がちがっても比較**できます。"),

    md("## 5. 偏差値\n\n"
       "z得点を見やすくしたものが偏差値。**偏差値 = 50 + 10 × z**（平均50・σ10）。\n\n"
       "> ⚠️ 偏差値で「上位何%」が決まるのは**分布が正規分布に近いとき**の目安（偏差値70 ≈ 上位約2%）。"
       "分布が大きく偏ると崩れます。"),
    code("df['数学偏差値'] = 50 + 10 * (df['数学'] - df['数学'].mean()) / df['数学'].std()\n"
         "print('偏差値の平均と標準偏差:', df['数学偏差値'].mean().round(1), '/', df['数学偏差値'].std().round(1))\n"
         "df[['生徒ID', '数学', '数学偏差値']].sort_values('数学', ascending=False).head()"),

    md("## 🧠 確認テスト（自動採点）\n\n"
       "`ans = None` を**自分の計算で置き換えて実行**すると、その場で正誤が出ます（答えは表示されません）。"),
    code("# まず採点用の関数を実行（答え合わせに使うだけ）\n"
         "def _check(name, got, want, tol=1e-2):\n"
         "    if got is None:\n"
         "        print(f'⬜ {name}: まだ入力されていません（ans を埋めてね）'); return\n"
         "    ok = abs(got - want) <= tol\n"
         "    print(('✅ 正解！ ' if ok else '❌ もう一度 ') + f'{name}: あなたの答え = {got}')"),
    code("# Q1: y = [2,4,6,8,10] の「分散（÷n, 母分散）」を計算して ans に入れよう\n"
         "y = np.array([2, 4, 6, 8, 10])\n"
         "ans = None     # 例: np.var(y)\n"
         "_check('Q1 分散(÷n)', ans, np.var(y))"),
    code("# Q2: 同じ y の「不偏分散（÷(n-1)）」を ans に入れよう\n"
         "ans = None     # ヒント: np.var(y, ddof=1) でもOK\n"
         "_check('Q2 不偏分散', ans, np.var(y, ddof=1))"),
    code("# Q3: 平均50・標準偏差10のテストで「70点」の z得点 を ans に入れよう\n"
         "ans = None     # ヒント: (70 - 50) / 10\n"
         "_check('Q3 z得点', ans, (70 - 50) / 10)"),

    md("---\n## 🏆 練習問題\n\n"
       "**問1.** `国語` の **不偏標準偏差** と **変動係数（標準偏差÷平均）** を求めよう。\n\n"
       "**問2.** `英語` を標準化した列を作り、平均≈0・標準偏差≈1 になることを確かめよう。\n\n"
       "**問3.** ある生徒は数学・英語ともに **75点**。偏差値が高いのはどちらの教科？ 理由も。\n\n"
       "**問4.（記述）** なぜ「分散そのもの」ではなく「分散の√＝標準偏差」を使うのか、*単位*の観点で説明しよう。"),
    code("# 問1〜3（問4は記述）\n"),
    md("> 🔑 **解答例（解説つき）は別ページ**（ネタバレ防止）。\n"
       "> 自分で解いてから 👉 **[01_分散・標準偏差・標準化 の解答例を開く]"
       "(https://github.com/Carlo-Broschi/statistics-python-for-students/blob/main/"
       "%E8%A7%A3%E7%AD%94%E9%9B%86/03_%E7%B5%B1%E8%A8%88_3%E7%B4%9A/"
       "01_%E5%88%86%E6%95%A3%E3%83%BB%E6%A8%99%E6%BA%96%E5%81%8F%E5%B7%AE%E3%83%BB%E6%A8%99%E6%BA%96%E5%8C%96.md)**"),

    md("## 📒 用語集 ＆ チートシート\n\n"
       "| 用語 | 意味 | 式 |\n"
       "|---|---|---|\n"
       "| 偏差 | 各データ − 平均 | $x_i - \\bar{x}$ |\n"
       "| 分散（母） | 偏差²の平均（÷n） | $\\frac{1}{n}\\sum (x_i-\\bar{x})^2$ |\n"
       "| 不偏分散 | ÷(n-1) の分散 | $\\frac{1}{n-1}\\sum (x_i-\\bar{x})^2$ |\n"
       "| 標準偏差 | √分散（単位が元に戻る） | $\\sqrt{分散}$ |\n"
       "| 標準化(z) | 平均0・σ1にそろえる | $z=\\frac{x-\\bar{x}}{s}$ |\n"
       "| 偏差値 | zを見やすく | $50+10z$ |\n"
       "| 変動係数 | 相対的なばらつき | $\\frac{s}{\\bar{x}}$ |"),
    code("# ── コピペ用チートシート ──\n"
         "s = df['数学']\n"
         "s.mean(); s.median()           # 平均・中央値\n"
         "s.var()                        # 不偏分散 ÷(n-1)\n"
         "s.std()                        # 不偏標準偏差\n"
         "np.var(s); np.std(s)           # 母分散・母標準偏差 ÷n\n"
         "(s - s.mean()) / s.std()       # 標準化（z得点）\n"
         "50 + 10 * (s - s.mean()) / s.std()   # 偏差値\n"
         "s.std() / s.mean()             # 変動係数"),
]
write_nb(NB, cells)

ans_md = """# 01_分散・標準偏差・標準化 ― 解答例（解説つき）

> 本編: [03_統計_3級/01_分散・標準偏差・標準化.ipynb](https://github.com/Carlo-Broschi/statistics-python-for-students/blob/main/03_%E7%B5%B1%E8%A8%88_3%E7%B4%9A/01_%E5%88%86%E6%95%A3%E3%83%BB%E6%A8%99%E6%BA%96%E5%81%8F%E5%B7%AE%E3%83%BB%E6%A8%99%E6%BA%96%E5%8C%96.ipynb)

**問1. 国語の不偏標準偏差と変動係数**
```python
print(df['国語'].std())                       # 不偏標準偏差（÷(n-1)）
print(df['国語'].std() / df['国語'].mean())    # 変動係数
```
💡 **なぜ変動係数？** 標準偏差は単位つき（点）なので、平均の大きさがちがうデータ同士の
「散らばりの大きさ」は単純に比べられない。平均で割って**相対化**すると比較できる。

**問2. 英語を標準化 → 平均0・標準偏差1**
```python
z = (df['英語'] - df['英語'].mean()) / df['英語'].std()
print(round(z.mean(), 5), round(z.std(), 3))   # ≈ 0, ≈ 1
```
💡 標準化は「平均を引いて標準偏差で割る」操作。**定義そのものから**、変換後は必ず
平均0・標準偏差1になる（だから単位の違うデータを同じ土俵で比べられる）。

**問3. 同じ75点、偏差値が高いのは？**
```python
for c in ['数学', '英語']:
    print(c, round(50 + 10 * (75 - df[c].mean()) / df[c].std(), 1))
```
💡 同じ75点でも、**平均が低く・σが小さい科目**ほど偏差値は高い。
偏差値は「点数の絶対値」ではなく「集団の中での相対位置」を表すから。

**問4.（記述）なぜ標準偏差を使う？**
分散は偏差を2乗するので単位が「元の単位の2乗」（例：点²）になり、現実の量として解釈しづらい。
√をとって標準偏差にすると単位が元（点）に戻り、「平均からおおよそ何点ズレているか」と
**直感的に解釈できる**。だから散らばりの代表的な指標には標準偏差を使う。
"""
os.makedirs(os.path.dirname(os.path.join(ROOT, ANS)), exist_ok=True)
open(os.path.join(ROOT, ANS), "w", encoding="utf-8").write(ans_md)
print("flagship done:", NB)
