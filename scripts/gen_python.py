from nbbuild import md, code, write_nb

# =====================================================================
# 01 Pythonをはじめよう
# =====================================================================
cells = [
    md("# 01. Pythonをはじめよう\n\n"
       "**このノートのゴール**\n"
       "- Jupyter Notebook の使い方がわかる\n"
       "- `print()` で文字や数を表示できる\n"
       "- 計算の答えをコンピュータに出してもらえる\n"
       "- エラーが出ても落ち着いて読めるようになる\n\n"
       "> 📒 セルを選んで **Shift + Enter** を押すと、そのセルが実行されます。やってみよう！"),
    md("## 1. はじめての `print`\n\n"
       "コンピュータに文字を表示してもらうには `print()` を使います。\n"
       "表示したい文字は **ダブルクォート `\"`** で囲みます。"),
    code('print("こんにちは、Python！")'),
    code('print("わたしの名前は ○○ です")  # ○○ を自分の名前に変えてみよう'),
    md("`#` から後ろは **コメント** です。プログラムには影響せず、メモとして使えます。"),
    md("## 2. Pythonは電卓になる\n\n"
       "数の計算もできます。記号の意味はこちら。\n\n"
       "| 記号 | 意味 | 例 |\n"
       "|---|---|---|\n"
       "| `+` | たし算 | `3 + 4` |\n"
       "| `-` | ひき算 | `10 - 2` |\n"
       "| `*` | かけ算 | `6 * 7` |\n"
       "| `/` | わり算 | `10 / 4` |\n"
       "| `//` | わり算の商（整数） | `10 // 4` |\n"
       "| `%` | わり算のあまり | `10 % 4` |\n"
       "| `**` | べき乗 | `2 ** 10` |"),
    code("3 + 4"),
    md("> 💡 セルの**最後の行**の値は、`print` を書かなくても自動で表示されます。"),
    code("print(10 / 4)   # わり算は小数になる\n"
         "print(10 // 4)  # 商\n"
         "print(10 % 4)   # あまり\n"
         "print(2 ** 10)  # 2の10乗"),
    md("### 計算の順番\n"
       "算数と同じで、かけ算・わり算が先。順番を変えたいときは `()` を使います。"),
    code("print(2 + 3 * 4)     # = 14\n"
         "print((2 + 3) * 4)   # = 20"),
    md("## 3. エラーはこわくない\n\n"
       "わざと間違えてみましょう。下のセルを実行するとエラーが出ます。"),
    code('print("とじカッコを忘れた"'),
    md("赤い文字の最後の行に注目！ `SyntaxError`（書き方の間違い）と書いてあります。\n"
       "**エラーメッセージの最後の行を読む**のがデバッグの第一歩です。\n\n"
       "上のセルを `print(\"とじカッコを忘れた\")` に直して、もう一度実行してみよう。"),
    md("---\n## 🏆 練習問題\n\n"
       "**問1.** あなたの好きな食べ物を `print` で表示しよう。\n\n"
       "**問2.** 「1年は何分か」を計算で求めて表示しよう（ヒント：365日 × 24時間 × 60分）。\n\n"
       "**問3.** `2026 % 4` を計算しよう。あまりが 0 ならうるう年の候補です。2026年はどうかな？"),
    code("# 問1\n"),
    code("# 問2\n"),
    code("# 問3\n"),
    md("<details><summary>解答例（クリックで開く）</summary>\n\n"
       "```python\n"
       "print(\"カレーライス\")\n"
       "print(365 * 24 * 60)   # 525600 分\n"
       "print(2026 % 4)        # 2 → うるう年ではない\n"
       "```\n</details>"),
]
write_nb("01_Python基礎/01_Pythonをはじめよう.ipynb", cells)

# =====================================================================
# 02 変数・数・文字列
# =====================================================================
cells = [
    md("# 02. 変数・数・文字列\n\n"
       "**ゴール**\n"
       "- 変数に値を入れて使い回せる\n"
       "- 整数・小数・文字列・真偽値の違いがわかる\n"
       "- 文字列を組み立てて表示できる（f-string）"),
    md("## 1. 変数 = 名前のついた箱\n\n"
       "`名前 = 値` で、値に名前をつけて保存できます。"),
    code('name = "山田はなこ"\n'
         "age = 15\n"
         "height = 158.5\n"
         "print(name, age, height)"),
    md("変数はあとから中身を変えたり、計算に使ったりできます。"),
    code("age = age + 1   # 1歳ふえた\n"
         "print(age)"),
    md("## 2. データの種類（型）\n\n"
       "値には **型** があります。`type()` で調べられます。\n\n"
       "| 型 | 意味 | 例 |\n"
       "|---|---|---|\n"
       "| `int` | 整数 | `15` |\n"
       "| `float` | 小数 | `158.5` |\n"
       "| `str` | 文字列 | `\"はなこ\"` |\n"
       "| `bool` | 真偽 | `True` / `False` |"),
    code("print(type(15))\n"
         "print(type(158.5))\n"
         'print(type("はなこ"))\n'
         "print(type(True))"),
    md("### 型を変える（キャスト）\n"
       "`input()` で受け取った値は文字列になります。計算するには数に変換が必要です。"),
    code('moji = "100"\n'
         "print(moji + moji)        # 文字列の足し算は連結 → 100100\n"
         "print(int(moji) + 50)     # 数にすれば計算できる → 150"),
    md("## 3. 文字列を組み立てる（f-string）\n\n"
       "`f\"...{変数}...\"` と書くと、`{}` の中に変数の値が入ります。とても便利！"),
    code('name = "はなこ"\n'
         "age = 15\n"
         'print(f"{name}さんは{age}歳です。来年は{age + 1}歳。")'),
    md("### 文字列の便利ワザ"),
    code('s = "Python"\n'
         "print(len(s))        # 文字数 → 6\n"
         "print(s.upper())     # 大文字 → PYTHON\n"
         "print(s * 3)         # くり返し → PythonPythonPython\n"
         "print(s[0])          # 先頭の文字 → P"),
    md("---\n## 🏆 練習問題\n\n"
       "**問1.** `radius = 5` として、半径5cmの円の面積（`半径 × 半径 × 3.14`）を計算し、\n"
       "「面積は ○○ 平方cmです」と f-string で表示しよう。\n\n"
       "**問2.** 自分の名前 `name` と生まれ年 `birth = 2010` を変数にし、\n"
       "「2026年に○○歳になります」と表示しよう。\n\n"
       "**問3.** `\"あ\" * 10` を実行して結果を予想してから確かめよう。"),
    code("# 問1\n"),
    code("# 問2\n"),
    code("# 問3\n"),
    md("<details><summary>解答例</summary>\n\n"
       "```python\n"
       "radius = 5\n"
       "area = radius * radius * 3.14\n"
       "print(f\"面積は{area}平方cmです\")\n\n"
       "name = \"はなこ\"\n"
       "birth = 2010\n"
       "print(f\"{name}さんは2026年に{2026 - birth}歳になります\")\n"
       "```\n</details>"),
]
write_nb("01_Python基礎/02_変数・数・文字列.ipynb", cells)

# =====================================================================
# 03 リスト・辞書・繰り返し
# =====================================================================
cells = [
    md("# 03. リスト・辞書・くり返し\n\n"
       "**ゴール**\n"
       "- たくさんの値をリストでまとめて扱える\n"
       "- 辞書で「名前と値」を対応づけられる\n"
       "- `for` で同じ処理をくり返せる\n\n"
       "ここは統計の章でデータを扱う土台になります。"),
    md("## 1. リスト＝値をならべた箱\n\n"
       "`[ ]` の中にカンマ区切りでならべます。"),
    code("scores = [60, 75, 90, 55, 80]\n"
         "print(scores)\n"
         "print(scores[0])     # 1番目（番号は0から！）\n"
         "print(scores[-1])    # 最後\n"
         "print(len(scores))   # 個数"),
    md("### リストの計算\n"
       "合計・最大・最小・平均などがすぐ出せます。"),
    code("print(sum(scores))            # 合計\n"
         "print(max(scores))            # 最大\n"
         "print(min(scores))            # 最小\n"
         "print(sum(scores) / len(scores))  # 平均"),
    md("### リストに追加・並べ替え"),
    code("scores.append(100)   # 末尾に追加\n"
         "print(scores)\n"
         "scores.sort()        # 小さい順に並べ替え\n"
         "print(scores)"),
    md("## 2. 辞書＝名前と値のペア\n\n"
       "`{キー: 値}` の形。名前（キー）で値を取り出せます。"),
    code('student = {"名前": "はなこ", "数学": 90, "英語": 75}\n'
         'print(student["名前"])\n'
         'print(student["数学"])'),
    md("## 3. for でくり返す\n\n"
       "リストの要素を1つずつ取り出して、同じ処理をします。"),
    code("for s in scores:\n"
         "    print(s, '点')"),
    md("> ⚠️ `for` の次の行は **4つの空白で字下げ（インデント）** します。Jupyterは自動でやってくれます。"),
    md("### range でくり返し回数を決める"),
    code("for i in range(5):    # 0,1,2,3,4\n"
         "    print(i, 'かいめ')"),
    md("### 例：60点以上が何人いるか数える"),
    code("scores = [60, 75, 90, 55, 80, 100, 45]\n"
         "count = 0\n"
         "for s in scores:\n"
         "    if s >= 60:\n"
         "        count = count + 1\n"
         "print('合格者は', count, '人')"),
    md("---\n## 🏆 練習問題\n\n"
       "**問1.** `temps = [22, 25, 19, 30, 28, 24, 21]`（1週間の気温）の平均を求めよう。\n\n"
       "**問2.** 上の `temps` のうち、25度以上だった日が何日あるか数えよう。\n\n"
       "**問3.** 辞書 `prices = {\"りんご\": 120, \"みかん\": 80, \"バナナ\": 100}` を作り、\n"
       "`for` で「りんごは120円」のように全部表示しよう（ヒント：`for k, v in prices.items():`）。"),
    code("# 問1\n"),
    code("# 問2\n"),
    code("# 問3\n"),
    md("<details><summary>解答例</summary>\n\n"
       "```python\n"
       "temps = [22, 25, 19, 30, 28, 24, 21]\n"
       "print(sum(temps) / len(temps))\n\n"
       "count = 0\n"
       "for t in temps:\n"
       "    if t >= 25:\n"
       "        count += 1\n"
       "print(count, '日')\n\n"
       "prices = {\"りんご\": 120, \"みかん\": 80, \"バナナ\": 100}\n"
       "for k, v in prices.items():\n"
       "    print(f\"{k}は{v}円\")\n"
       "```\n</details>"),
]
write_nb("01_Python基礎/03_リスト・辞書・くり返し.ipynb", cells)

# =====================================================================
# 04 条件分岐・関数・モジュール
# =====================================================================
cells = [
    md("# 04. 条件分岐・関数・ライブラリ\n\n"
       "**ゴール**\n"
       "- `if` で場合分けができる\n"
       "- `def` で自分の命令（関数）を作れる\n"
       "- `import` で便利な道具（ライブラリ）を使える\n\n"
       "ここまでできれば、統計の章にすすめます！"),
    md("## 1. if で場合分け\n\n"
       "条件が `True` のときだけ実行されます。"),
    code("score = 72\n"
         "if score >= 80:\n"
         "    print('すごい！')\n"
         "elif score >= 60:\n"
         "    print('合格')\n"
         "else:\n"
         "    print('もう少し')"),
    md("### 比較の記号\n\n"
       "| 記号 | 意味 |\n|---|---|\n"
       "| `==` | 等しい |\n| `!=` | 等しくない |\n"
       "| `>` `>=` | 大きい / 以上 |\n| `<` `<=` | 小さい / 以下 |\n\n"
       "`and`（かつ）、`or`（または）で組み合わせられます。"),
    code("temp = 28\n"
         "if temp >= 25 and temp < 30:\n"
         "    print('夏日です')"),
    md("## 2. 関数＝自分で作る命令\n\n"
       "同じ処理を何度も使うときは関数にまとめます。"),
    code("def greet(name):\n"
         "    return f'こんにちは、{name}さん！'\n\n"
         "print(greet('はなこ'))\n"
         "print(greet('たろう'))"),
    md("### 例：平均を返す関数"),
    code("def average(numbers):\n"
         "    return sum(numbers) / len(numbers)\n\n"
         "print(average([60, 70, 80]))\n"
         "print(average([100, 90]))"),
    md("## 3. ライブラリを import する\n\n"
       "Pythonには便利な道具がたくさん付いてきます。`import` で呼び出します。"),
    code("import math\n"
         "print(math.sqrt(144))   # 平方根 → 12.0\n"
         "print(math.pi)          # 円周率\n\n"
         "import random\n"
         "print(random.randint(1, 6))  # サイコロ（1〜6のランダム）"),
    md("> 📦 統計の章では `numpy`（数値計算）、`pandas`（表データ）、`matplotlib`（グラフ）を使います。\n"
       "これらも `import` で呼び出すだけです。"),
    md("---\n## 🏆 まとめ練習\n\n"
       "**問1.** 点数を受け取り、80以上なら `'A'`、60以上なら `'B'`、それ未満なら `'C'` を返す\n"
       "関数 `grade(score)` を作ろう。\n\n"
       "**問2.** サイコロを2回ふって合計を表示するプログラムを書こう。\n\n"
       "**問3.**（チャレンジ）サイコロを1000回ふって、出た目の平均を求めよう。\n"
       "理論上の平均 3.5 に近くなるかな？"),
    code("# 問1\n"),
    code("# 問2\n"),
    code("# 問3\n"),
    md("<details><summary>解答例</summary>\n\n"
       "```python\n"
       "def grade(score):\n"
       "    if score >= 80:\n"
       "        return 'A'\n"
       "    elif score >= 60:\n"
       "        return 'B'\n"
       "    else:\n"
       "        return 'C'\n"
       "print(grade(85), grade(70), grade(40))\n\n"
       "import random\n"
       "a = random.randint(1, 6)\n"
       "b = random.randint(1, 6)\n"
       "print(a, b, '合計', a + b)\n\n"
       "total = 0\n"
       "for i in range(1000):\n"
       "    total += random.randint(1, 6)\n"
       "print(total / 1000)\n"
       "```\n</details>\n\n"
       "🎉 **Python基礎クリア！** 次は `02_統計_4級` フォルダへ進もう。"),
]
write_nb("01_Python基礎/04_条件分岐・関数・ライブラリ.ipynb", cells)

print("=== Python基礎 done ===")
