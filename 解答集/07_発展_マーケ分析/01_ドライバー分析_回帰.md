# 01_ドライバー分析（回帰）― 解答例

> 本編: [07_発展_マーケ分析/01_ドライバー分析_回帰.ipynb](https://github.com/Carlo-Broschi/statistics-python-for-students/blob/main/07_%E7%99%BA%E5%B1%95_%E3%83%9E%E3%83%BC%E3%82%B1%E5%88%86%E6%9E%90/01_%E3%83%89%E3%83%A9%E3%82%A4%E3%83%90%E3%83%BC%E5%88%86%E6%9E%90_%E5%9B%9E%E5%B8%B0.ipynb)

```python
# 問1: チャネル別の平均単価
print(smf.ols('商談金額 ~ C(獲得チャネル)', data=btob).fit().params.round(0))

# 問2: 業界を加える
l2 = smf.logit('受注 ~ 商談金額 + C(獲得チャネル) + C(業界)', data=btob).fit()
print(l2.prsquared)   # 元(0.089)とほぼ同じ→業界は受注にあまり効かない

# 問3: テレアポのオッズ比
print(np.exp(logit.params['C(獲得チャネル)[T.テレアポ]']))  # ≈1.4倍

# 問4: 担当者別の受注率
print(btob.groupby('担当者')['受注'].mean().round(3))
```
