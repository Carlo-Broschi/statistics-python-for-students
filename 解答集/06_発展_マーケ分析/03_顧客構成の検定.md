# 03_顧客構成の検定 ― 解答例

```python
from scipy import stats
# 問1: 想定シェアを変えて結論を見る
plan2={'IT':0.20,'金融':0.20,'小売':0.15,'医療':0.15,'製造':0.15,'建設':0.15}
exp=[len(cust)*plan2[i] for i in obs.index]
print(stats.chisquare(obs.values, exp))  # 期待に近いほどp値は大きく「想定どおり」

# 問2: 独立性検定（2つの質的列があれば）
# 例: 業界 × （正解セグメント）
tab=pd.crosstab(cust['業界'], cust['（正解セグメント）'])
print(stats.chi2_contingency(tab)[:2])  # chi2, p
```

問3（考察）：適合度で「有意でない」ときは「想定（または均等）と異なるとは言えない」と報告する。「想定どおりと証明された」ではない点に注意（差を検出できなかっただけの可能性）。サンプルを増やすと結論が変わることもある。
