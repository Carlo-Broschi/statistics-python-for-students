# 01_顧客指標の相関分析 ― 解答例

```python
import numpy as np
# 問1: 累計売上×Recency の単純相関と、購入回数を制御した偏相関
def partial_corr(x,y,z,d):
    rxy,rxz,ryz=d[x].corr(d[y]),d[x].corr(d[z]),d[y].corr(d[z])
    return (rxy-rxz*ryz)/np.sqrt((1-rxz**2)*(1-ryz**2))
print('単純', round(cust['累計売上'].corr(cust['Recency']),3))
print('偏相関(購入回数制御)', round(partial_corr('累計売上','Recency','購入回数',cust),3))

# 問2: 散布図行列
import pandas as pd
pd.plotting.scatter_matrix(cust[['購入回数','累計売上','Recency']], figsize=(7,7)); plt.show()
```

問3（考察）：例＝アイスの売上と熱中症の件数。両方とも「気温」という第3の要因で増えるため相関が出るが、一方が他方を起こしているわけではない（見せかけの相関）。
